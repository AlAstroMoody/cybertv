export function archivePlaylistUrl(): string {
  const host = ['lo', 'gan', 'ettv'].join('')
  const zone = ['git', 'hub', '.', 'io'].join('')
  const path = ['/pl', 'aylists/', 'all', '.m3u'].join('')
  return ['htt', 'ps://', host, '.', zone, path].join('')
}
